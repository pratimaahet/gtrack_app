/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package gtrack_app.app;

import gtrack_app.list.LinkedList;

import static gtrack_app.utilities.StringUtils.join;
import static gtrack_app.utilities.StringUtils.split;
import static gtrack_app.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
